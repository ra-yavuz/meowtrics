; Add this module to ~/.config/polybar/config.ini, then reference it in
; the modules-left/center/right list of your bar.

[module/meowtrics]
type = custom/script
exec = meowtrics status --short
interval = 5
format = <label>
label =  %output%
label-foreground = #ECECEC
